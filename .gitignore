# Local working folders — not uploaded to GitHub
_local/
_notes/
_archive/
_temp/

# Local firebase-tools install (added for the Firebase MCP server)
node_modules/

# Machine-local MCP server config — hardcodes this machine's absolute paths
# and the Firebase service-account credential path
.mcp.json
